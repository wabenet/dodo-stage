---
stages:
  test:
    type: someplugin
    box:
      user: debian
      name: buster64
      version: 10.0.0
    resources:
      cpu: 1
      memory: 200MB
      volumes:
        - size: 100GB
    options:
      foo: bar
